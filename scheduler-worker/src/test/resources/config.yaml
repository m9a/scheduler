coordinator:
  host: localhost
  port: 0
  heartbeatIntervalSeconds: 5
  pollIntervalSeconds: 5

hostname: localhost
port: 0

checkpointPath: target/worker_checkpoint.yaml

jobExecutionTimeoutMinutes: 10

resources:
  memory: 8192
  cpu: 8
