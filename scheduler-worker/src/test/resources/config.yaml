coordinator:
  host: localhost
  port: 0
  heartbeatIntervalSeconds: 5
  pollIntervalSeconds: 5

hostname: localhost
port: 0
metricsPort: 0

checkpointPath: target/worker_checkpoint.yaml
statusDbPath: target/worker_status.db
statusRetentionDays: 7

resources:
  memory: 8192
  cpu: 8
